<%= form_for([@supercar]) do |f| %>

  <% if @supercar.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@supercar.errors.count, "error") %> prohibited this supercar from being saved:</h2>

      <ul>
      <% @supercar.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      <% end %>


  <%= fields_for(@supercar.make) do |m| %>
 <div class="field">
   <%= m.label :Make %><br />
   <%= m.text_field :name %>
 </div>
 <% end %>
 <%= fields_for(@supercar.car_model) do |c| %>
 <div class="field">
   <%= c.label :Model %><br />
   <%= c.text_field :name %>
 </div>
 <% end %>
 <%= fields_for(@supercar.city) do |i| %>
 <div class="field">
   <%= i.label :City %><br />
   <%= i.text_field :name %>
 </div>
 <% end %>
 <%= fields_for(@supercar.country) do |o| %>
 <div class="field">
   <%= o.label :Country %><br />
   <%= o.text_field :name %>
 </div>
 <% end %>
 <div class="field">
   <%= f.label :supercar_url %><br />
   <%= f.text_field :supercar_url %>
 </div>
 <div class="actions">
   <%= f.submit %>
 </div>
 <% end %>



